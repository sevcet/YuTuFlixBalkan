<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 1. PERMISSIONS (već imaš) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <!-- 2. HARDWARE FEATURES – obavezno za TV -->
    <uses-feature android:name="android.hardware.accelerated" android:required="false"/>
    <uses-feature android:name="android.hardware.touchscreen"      android:required="false"/>
    <uses-feature android:name="android.software.leanback"         android:required="true"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YuTUFlix">

        <!-- AdMob -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9646614878051651~8270354441"/>

        <!--=========== MOBILE LAUNCHER ===========-->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--=========== TV LAUNCHER (samo u tv flavor) ===========-->
        <activity
            android:name=".tv.TvActivity"
            android:banner="@drawable/banner_320x180"
            android:icon="@drawable/banner_320x180"
            android:logo="@drawable/banner_320x180"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Ostale aktivnosti -->
        <activity android:name=".DetailsActivityFilm"/>
        <activity android:name=".DetailsActivitySeries"/>
        <activity
            android:name=".PlayerActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:screenOrientation="landscape"/>
        <activity android:name=".CategoryActivity"/>
        <activity android:name=".PrivacyActivity"/>
        <activity
            android:name=".FavoritesActivity"
            android:exported="false"/>
    </application>
</manifest>